# これは何か

`fridge-to-meal` リポジトリに入れる予定のファイル一式の**控え**です。
セッションのコンテナは破棄されるため、消失を防ぐ目的でここに置いています。

`fridge-to-meal` リポジトリを作成して push したら、**このディレクトリごと削除してください。**

HTML 版（`../domain-model.html` / `../adr.html`）はこの階層には複製していません。
新リポジトリでは `docs/html/` に配置されます。
